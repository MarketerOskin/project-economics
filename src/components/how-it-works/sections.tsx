import { FolderSync, SplitSquareVertical, Percent, Clock3, ShieldCheck, History } from 'lucide-react';

export const HOW_IT_WORKS_SECTIONS: {
  icon: React.ComponentType<{ className?: string }>;
  title: string;
  body: string[];
}[] = [
  {
    icon: FolderSync,
    title: 'Откуда берутся проекты',
    body: [
      'Проект можно создать вручную или импортом из сделки/компании Bitrix24 CRM.',
      'Связь с CRM — снимок на момент импорта (название и ссылка), а не живая связь: если сделку в Bitrix24 переименуют, переместят или удалят, у проекта останутся те данные, что были на момент импорта. Расчёты экономики от доступности CRM не зависят.',
    ],
  },
  {
    icon: SplitSquareVertical,
    title: 'План и факт — отдельные записи',
    body: [
      'Плановая и фактическая операции — это две разные записи, а не два числа в одной строке. Поэтому план можно внести заранее, а факт — позже, отдельным действием, и увидеть отклонение между ними.',
      'Фактическую операцию можно (не обязательно) привязать к конкретной плановой — тогда в истории видно, какой план она закрывает.',
    ],
  },
  {
    icon: Percent,
    title: 'Как считаются прибыль и маржа',
    body: [
      'Прибыль = доход − расход.',
      'Маржа = прибыль / доход × 100. Если дохода ещё нет (равен нулю), маржа не 0% и не ошибка — она не определена и показывается как «—».',
      'Разница между фактической и плановой маржой указывается в процентных пунктах (п.п.), а не в процентах — это разные единицы, и их легко перепутать.',
    ],
  },
  {
    icon: Clock3,
    title: 'Часы и ставка',
    body: [
      'При почасовом начислении сумму всегда пересчитывает сервер (часы × ставка на дату операции) — значение, присланное из браузера, никогда не используется напрямую. Это защита от случайной или намеренной подмены суммы.',
    ],
  },
  {
    icon: ShieldCheck,
    title: 'Кто что видит',
    body: [
      'Сотрудник (EMPLOYEE) видит только те проекты, где указан участником.',
      'Руководитель (MANAGER) и администратор (ADMIN) видят все проекты и всю экономику компании.',
      'Назначать роли другим сотрудникам и менять настройки интеграции может только администратор.',
      'Права всегда проверяются на сервере при каждом запросе — то, что скрыто в интерфейсе, не является защитой само по себе.',
    ],
  },
  {
    icon: History,
    title: 'Удаление — мягкое',
    body: [
      'Удалённая операция сразу пропадает из всех сумм и графиков, но не стирается физически: она остаётся в истории и журнале аудита с пометкой, кто и когда её удалил.',
    ],
  },
];

export function HowItWorksSections() {
  return (
    <div className="flex flex-col gap-4 px-4 py-6 sm:px-8">
      {HOW_IT_WORKS_SECTIONS.map(({ icon: Icon, title, body }) => (
        <section key={title} className="rounded-[16px] border border-border bg-surface p-5">
          <div className="flex items-center gap-2.5">
            <span className="flex size-8 items-center justify-center rounded-full bg-accent-soft text-accent">
              <Icon className="size-4" />
            </span>
            <h2 className="text-[15px] font-medium text-fg">{title}</h2>
          </div>
          <div className="mt-3 flex flex-col gap-2 pl-[42px]">
            {body.map((p, i) => (
              <p key={i} className="text-sm text-fg-secondary">
                {p}
              </p>
            ))}
          </div>
        </section>
      ))}
    </div>
  );
}
